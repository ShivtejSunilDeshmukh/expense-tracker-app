apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: expense-tracker
  namespace: argocd

spec:

  # Argo CD project
  project: default

  # ==========================================================
  # Git repository containing Kubernetes manifests
  # ==========================================================

  source:
    repoURL: https://github.com/ShivtejSunilDeshmukh/expense-tracker-app.git
    targetRevision: main
    path: K8s

  # ==========================================================
  # Kubernetes destination
  # ==========================================================

  destination:
    server: https://kubernetes.default.svc
    namespace: expense-tracker

  # ==========================================================
  # Automatic GitOps synchronization
  # ==========================================================

  syncPolicy:

    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=false
