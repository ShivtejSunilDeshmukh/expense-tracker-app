apiVersion: v1
kind: Service

metadata:
  name: expense-tracker
  namespace: expense-tracker

spec:

  type: ClusterIP

  selector:
    app: expense-tracker

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
