apiVersion: v1
kind: Service

metadata:
  name: expense-tracker
  namespace: expense-tracker

spec:
  type: LoadBalancer

  selector:
    app: expense-tracker

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
